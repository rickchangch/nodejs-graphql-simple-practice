# nodejs-practice

## Folder structure
```
    .
    ├── pure_nodejs         # test pure NodeJS feature
    │   ├── event_loop        # observe the priority of common asynchronous method in event loop mechanism
    │   └── http_server       # built-in http module
    ├── web_application     # build basic backend server through Express.js for implementing REST API
    └── graphql_be_server   # build GraphQL API gateway as a adapter between client-side and REST API server
```
